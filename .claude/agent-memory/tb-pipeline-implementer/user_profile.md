---
name: user_profile
description: User background, role, and collaboration preferences for the TB pipeline project
type: user
---

The user is a student working on a deep learning final project (semester 6) focused on TB detection from chest X-rays. The project involves implementing a full multi-component inference and training pipeline in PyTorch.

The user appears to work primarily via a critic-agent/implementer-agent workflow: a critic agent audits the codebase and produces a structured report, then the implementer (this agent) acts on every item in that report.

Codebase lives on Windows (OneDrive path with spaces). Python 3.14 is the system Python with torch 2.11 and pydantic 2.13 installed. The project venv (.venv/) does not have pytest or torch installed — system Python is used for testing.

The user's GitHub repo is: https://github.com/mabdullahi7780/dl-project-codebase
