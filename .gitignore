# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.venv/
venv/
.pytest_tmp/
pytest-cache-files-*/
component1_nih_eb594ru_/
# Jupyter
.ipynb_checkpoints/

# Local env
.env

# timika virtual environment (created by setup_timika_env.ps1)
timika/

# Artifacts
outputs/
logs/

# Data and checkpoints
data/raw/*
data/interim/*
data/processed/*
data/splits/*
data/cache/*
checkpoints/sam/*
checkpoints/medsam/*
checkpoints/txv/*
checkpoints/baseline/*
checkpoints/reports/*

# Component 1 adapters + training history live in the repo for baseline
# reproducibility, but the 361 MB full snapshot and 2 MB resume snapshot do not.
checkpoints/component1/*.pt
checkpoints/component1/last_*.pt

# Keep placeholder files
!data/raw/.gitkeep
!data/interim/.gitkeep
!data/processed/.gitkeep
!data/splits/.gitkeep
!data/cache/.gitkeep
!checkpoints/sam/.gitkeep
!checkpoints/medsam/.gitkeep
!checkpoints/txv/.gitkeep
!checkpoints/baseline/.gitkeep
!checkpoints/reports/.gitkeep

# Claude Code local state
.claude/

# macOS metadata embedded in extracted zips
**/__MACOSX/

# Local NIH index cache (regenerable, ~18 MB per ablation variant)
**/_nih_index_cache.json

# Re-buildable Overleaf bundle (sources + figures are tracked separately)
final_deliverable_report/final_deliverable_report/tb_paper_overleaf.zip
local_work

# Large result/artifact bundles — regenerable on Kaggle, never commit (they
# bloat the pack and break `git push`, e.g. the 453 MB checkpoints zip).
*.zip
tbportals_paper_baseline/
automated-timika-scoring-paper.pdf

# --- side-work push: never commit raw TB Portals data, feature caches, image dumps ---
.DS_Store
**/.DS_Store
TB_Portals_CXR_Manual_Annotations_August_2023.csv
TB_Portals_CXRs_August_2023.csv
notebooks/tbportals_manifest_paper.csv
features_rad-dino_*.npz
cavity_attn_pngs/
moldova_*.png